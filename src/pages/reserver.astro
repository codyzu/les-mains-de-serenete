---
import {defaultLocale, getLocalizedSite, site} from '../data/site';
import ReserveRedirectPageTemplate from '../page-templates/ReserveRedirectPageTemplate.astro';

const localized = getLocalizedSite(defaultLocale);
---

<ReserveRedirectPageTemplate
  locale={defaultLocale}
  business={site.business}
  localized={localized}
/>
